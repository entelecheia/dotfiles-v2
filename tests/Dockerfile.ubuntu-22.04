FROM ubuntu:22.04
ENV DEBIAN_FRONTEND=noninteractive
RUN apt-get update && apt-get install -y \
    sudo curl git zsh ca-certificates findutils coreutils \
    && rm -rf /var/lib/apt/lists/*
RUN useradd -m -s /bin/bash testuser \
    && echo "testuser ALL=(ALL) NOPASSWD:ALL" >> /etc/sudoers
COPY dot /usr/local/bin/dot
RUN chmod +x /usr/local/bin/dot && ln -sf /usr/local/bin/dot /usr/local/bin/dotfiles
COPY scripts/ /scripts/
COPY tests/ /tests/
RUN chmod +x /tests/*.sh && find /tests/scenarios -name '*.sh' -exec chmod +x {} +
USER testuser
WORKDIR /home/testuser
