# Static exclude rules for `dot gdrive-sync`.
#
# Layered with two other mechanisms:
#   - `.dotfiles/gdrive-sync/ignore.txt` for operator-specific excludes
#   - `--no-links` skips all symlinks
# `.gitignore` is intentionally not a gdrive-sync filter: Git-ignored
# binaries are the main payload class this command is meant to cover.
# This file is the always-on baseline.

# git internals — match file AND directory at any depth.
# Submodules use a `.git` *file* (gitlink) not a directory, so the
# slash-only patterns from the parent file alone are insufficient.
.git
.git/
.git/**
.gitmodules
**/.git
**/.git/

# claude / omc local state
.omc/
.omx/
.claude/settings.local.json
**/.claude/settings.local.json

# legacy dual-path artifacts
.gdrive
.gdrive/

# Note: shared-folder exclusions are NOT name-based here.
# Drive shortcut detection (.shortcut-targets-by-id, Shared drives/) and
# the operator's manual list (state.modules.gdrive_sync.shared_excludes)
# are both layered in at sync-time via a second --exclude-from file.
# See `dot gdrive-sync shared`.

# rsync self-protection (never sync the conflict backups themselves)
.sync-conflicts/

# Drive metadata / OS files
.DS_Store
._*
Icon?

# Google Docs native (workspace never edits these directly)
*.gdoc
*.gsheet
*.gslides
*.gdraw
*.gform
*.gshortcut
*.gtable
*.gmap
*.gsite

# Drive in-flight artifacts
*.tmp.driveupload
*.tmp.drivedownload

# build / cache directories
node_modules/
.venv/
__pycache__/
*.pyc
.pytest_cache/
target/
dist/
build/
.next/
.turbo/

# never sync the toolkit venv
_sys/env/.venv/
