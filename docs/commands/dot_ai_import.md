## dot ai import

Import AI settings from a portable tar.gz archive

```
dot ai import <file.tar.gz> [flags]
```

### Options

```
  -h, --help           help for import
      --include-auth   Import auth/local-secret files from the archive
```

### Options inherited from parent commands

```
      --config string    Path to custom config YAML
      --dry-run          Show what would be done without executing
      --home string      Override home directory (for admin setup of other users)
      --module strings   Run specific modules only
      --profile string   Profile name (minimal, full, server)
      --yes              Unattended mode (skip all prompts)
```

### SEE ALSO

* [dot ai](dot_ai.md)	 - AI CLI/config helpers and settings backup/restore

