## dot ai agents show

Print the raw or rendered agents SSOT

```
dot ai agents show [flags]
```

### Options

```
  -h, --help                help for show
      --rendered string     Print SSOT rendered for one tool
      --with-line-numbers   Prefix output with line numbers
```

### Options inherited from parent commands

```
      --config string    Path to custom config YAML
      --dry-run          Show what would be done without executing
      --home string      Override home directory (for admin setup of other users)
      --module strings   Run specific modules only
      --profile string   Profile name (minimal, full, server)
      --yes              Unattended mode (skip all prompts)
```

### SEE ALSO

* [dot ai agents](dot_ai_agents.md)	 - Manage the shared AI agents instruction SSOT

