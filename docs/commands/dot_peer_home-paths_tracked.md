## dot peer home-paths tracked

Read or replace the tracked host-path list (deletes propagate, conflicts quarantine)

```
dot peer home-paths tracked [flags]
```

### Options

```
  -h, --help   help for tracked
```

### Options inherited from parent commands

```
      --config string    Path to custom config YAML
      --dry-run          Show what would be done without executing
      --home string      Override home directory (for admin setup of other users)
      --module strings   Run specific modules only
      --profile string   Profile name (minimal, full, server)
      --yes              Unattended mode (skip all prompts)
```

### SEE ALSO

* [dot peer home-paths](dot_peer_home-paths.md)	 - Read or replace the peer host-path allowlist
* [dot peer home-paths tracked get](dot_peer_home-paths_tracked_get.md)	 - Read the tracked peer host-path list
* [dot peer home-paths tracked set](dot_peer_home-paths_tracked_set.md)	 - Replace the tracked peer host-path list from stdin

